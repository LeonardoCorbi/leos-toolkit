on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: matrix ${{ matrix.FTP_HOST }} secret ${{ secret.FTP_HOST }}
      uses: actions/setup-node@v2
      with:
        node-version: '14'
      
    - name: 🔨 Build Project
      run: |
        npm i
        npm run build
      env:
        CI: false

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ matrix.FTP_HOST }}
        username: ${{ matrix.FTP_USERNAME }}
        password: ${{ matrix.FTP_PASSWORD }}
        local-dir: build/
        server-dir: ${{ matrix.FTP_PATH }}
